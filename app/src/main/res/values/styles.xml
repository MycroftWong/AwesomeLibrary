<resources>


    <!-- 通用的Toolbar样式 -->
    <style name="CommonToolbarStyle" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@color/colorPrimary</item>
        <item name="titleTextAppearance">@style/ToolbarTitleTextAppearance</item>
    </style>

    <!-- 通用Toolbar的标题样式 -->
    <style name="ToolbarTitleTextAppearance" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- 通用的TextView样式 -->
    <style name="CommonTextViewStyle" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/common_text_color</item>
        <item name="android:textSize">@dimen/common_text_size</item>
    </style>

    <style name="CommonEditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="android:textColor">@color/common_text_color</item>
        <item name="android:textSize">@dimen/common_text_size</item>
    </style>

    <!-- 通用的Button样式 -->
    <style name="CommonButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/bg_common_button</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- CustomTitleBar 样式设置 -->
    <style name="QDTopBar" parent="CustomTitleBar.TopBar">
        <item name="titlebar_bg_color">@android:color/white</item>
        <item name="titlebar_title_text_size">18dp</item>
        <item name="titlebar_title_color">@color/common_text_color</item>
        <item name="titlebar_subtitle_color">@color/config_color_white</item>
        <item name="titlebar_text_btn_color_state_list">@android:color/white</item>
        <item name="titlebar_image_btn_height">44dp</item>
    </style>

</resources>
